#  마크다운(Markdown) 완전 정복 가이드

> **프롬프트 엔지니어링 강의 보조 자료**  
> 이 문서는 마크다운을 처음 접하는 학생을 위한 문법 안내서이다.  
> 프롬프트 작성, GitHub 문서화, 노션 정리 등 다양한 곳에서 활용할 수 있다.

---

##  목차

1. [마크다운이란 무엇인가](#1-마크다운이란-무엇인가)
2. [어디서 쓸 수 있는가](#2-어디서-쓸-수-있는가)
3. [제목 (Headings)](#3-제목-headings)
4. [텍스트 강조 (Emphasis)](#4-텍스트-강조-emphasis)
5. [목록 (Lists)](#5-목록-lists)
6. [링크와 이미지 (Links & Images)](#6-링크와-이미지-links--images)
7. [코드 (Code)](#7-코드-code)
8. [인용문 (Blockquotes)](#8-인용문-blockquotes)
9. [표 (Tables)](#9-표-tables)
10. [수평선 (Horizontal Rules)](#10-수평선-horizontal-rules)
11. [체크리스트 (Task Lists)](#11-체크리스트-task-lists)
12. [접기/펼치기 (Collapsible Sections)](#12-접기펼치기-collapsible-sections)
13. [이모지 (Emoji)](#13-이모지-emoji)
14. [각주 (Footnotes)](#14-각주-footnotes)
15. [수식 (Math) — GitHub 지원](#15-수식-math--github-지원)
16. [프롬프트 엔지니어링에서의 마크다운 활용](#16-프롬프트-엔지니어링에서의-마크다운-활용)
17. [실전 종합 예제: README.md 작성하기](#17-실전-종합-예제-readmemd-작성하기)
18. [자주 하는 실수와 해결법](#18-자주-하는-실수와-해결법)
19. [치트시트 (한 장 요약)](#19-치트시트-한-장-요약)

---

## 1. 마크다운이란 무엇인가

마크다운(Markdown)은 2004년 존 그루버(John Gruber)가 만든 **경량 마크업 언어**이다. 일반 텍스트에 간단한 기호(`#`, `*`, `-` 등)를 추가하여 제목, 강조, 목록, 링크 등의 서식을 표현한다.

마크다운의 핵심 철학은 "읽기 쉬운 원문"이다. HTML처럼 복잡한 태그를 쓰지 않아도, 원본 텍스트 자체가 직관적으로 읽히도록 설계되어 있다.

```
HTML로 제목을 쓰면:     <h1>제목입니다</h1>
마크다운으로 쓰면:       # 제목입니다
```

둘 다 결과는 같지만, 마크다운 쪽이 훨씬 간결하고 읽기 쉽다.

---

## 2. 어디서 쓸 수 있는가

마크다운은 놀라울 정도로 많은 곳에서 지원된다.

| 플랫폼 | 활용 예시 |
|--------|----------|
| **GitHub** | README.md, Issue, Pull Request, Wiki |
| **노션 (Notion)** | 페이지 작성 시 마크다운 단축키 지원 |
| **Obsidian** | 개인 노트, 지식 관리 |
| **Jupyter Notebook** | 코드 셀 사이의 설명 텍스트 |
| **Discord / Slack** | 메시지 서식 (굵게, 코드 블록 등) |
| **ChatGPT / Claude** | 프롬프트 구조화, AI 응답 서식 |
| **VS Code** | `.md` 파일 편집 + 실시간 미리보기 |
| **블로그** | Jekyll, Hugo, Gatsby 등 정적 사이트 생성기 |

>  **프롬프트 엔지니어링과의 연결**: 강의 자료에서 다루었듯이, 마크다운은 프롬프트를 구조화하는 핵심 기법 중 하나이다. `#` 기호로 섹션을 나누고, `-`로 목록을 만들면 AI가 텍스트를 더 쉽게 처리할 수 있다.

---

## 3. 제목 (Headings)

`#` 기호의 개수로 제목의 레벨을 결정한다. `#`이 1개이면 가장 큰 제목(H1)이고, 6개이면 가장 작은 제목(H6)이다.

### 작성법

```markdown
# 제목 1 (H1) — 문서 전체 제목에 사용
## 제목 2 (H2) — 대분류
### 제목 3 (H3) — 중분류
#### 제목 4 (H4) — 소분류
##### 제목 5 (H5)
###### 제목 6 (H6)
```

### 렌더링 결과

> # 제목 1
> ## 제목 2
> ### 제목 3
> #### 제목 4

### 주의사항

- `#` 뒤에 **반드시 공백(스페이스)**을 넣어야 한다
- `#제목` (공백 없음) → 제목으로 인식되지 않는다
- `# 제목` (공백 있음) → 올바르게 제목으로 렌더링된다
- 하나의 문서에 H1(`#`)은 **하나만** 사용하는 것이 관례이다

---

## 4. 텍스트 강조 (Emphasis)

### 기본 강조

```markdown
이것은 *기울임(이탤릭)*이다.
이것은 **굵게(볼드)**이다.
이것은 ***굵은 기울임***이다.
이것은 ~~취소선~~이다.
```

**렌더링 결과**:

이것은 *기울임(이탤릭)*이다.  
이것은 **굵게(볼드)**이다.  
이것은 ***굵은 기울임***이다.  
이것은 ~~취소선~~이다.

### 요약표

| 작성법 | 결과 | 용도 |
|--------|------|------|
| `*텍스트*` 또는 `_텍스트_` | *기울임* | 용어 첫 등장, 강조 |
| `**텍스트**` 또는 `__텍스트__` | **굵게** | 핵심 키워드 강조 |
| `***텍스트***` | ***굵은 기울임*** | 매우 중요한 내용 |
| `~~텍스트~~` | ~~취소선~~ | 삭제된 내용, 오답 표시 |

>  **팁**: 실제로 가장 많이 쓰는 것은 `**굵게**`이다. 기울임은 영어 문서에서 자주 쓰이지만 한국어에서는 가독성이 떨어지므로 굵게를 더 많이 사용한다.

---

## 5. 목록 (Lists)

### 순서 없는 목록 (Unordered List)

`-`, `*`, `+` 중 아무 기호나 사용할 수 있다. 관례상 `-`를 가장 많이 사용한다.

```markdown
- 첫 번째 항목
- 두 번째 항목
  - 하위 항목 (스페이스 2칸 들여쓰기)
  - 하위 항목 2
    - 더 깊은 하위 항목 (스페이스 4칸)
- 세 번째 항목
```

**렌더링 결과**:

- 첫 번째 항목
- 두 번째 항목
  - 하위 항목
  - 하위 항목 2
    - 더 깊은 하위 항목
- 세 번째 항목

### 순서 있는 목록 (Ordered List)

숫자와 점(`.`)을 사용한다.

```markdown
1. 첫 번째 단계
2. 두 번째 단계
3. 세 번째 단계
   1. 세부 단계 a
   2. 세부 단계 b
```

**렌더링 결과**:

1. 첫 번째 단계
2. 두 번째 단계
3. 세 번째 단계
   1. 세부 단계 a
   2. 세부 단계 b

>  **재미있는 사실**: 실제로 번호를 틀리게 써도 마크다운이 자동으로 순서를 매겨준다. `1. 1. 1.`이라고 써도 렌더링하면 `1. 2. 3.`으로 표시된다. 하지만 가독성을 위해 올바른 번호를 쓰는 것이 좋다.

---

## 6. 링크와 이미지 (Links & Images)

### 링크

```markdown
[표시할 텍스트](URL)

예시:
[OpenAI Playground](https://platform.openai.com/playground)
[구글](https://www.google.com "마우스 올리면 보이는 설명")
```

**렌더링 결과**: [OpenAI Playground](https://platform.openai.com/playground)

### URL 직접 표시

```markdown
<https://platform.openai.com/playground>
```

**렌더링 결과**: <https://platform.openai.com/playground>

### 이미지

이미지는 링크 앞에 `!`를 붙이면 된다.

```markdown
![대체 텍스트](이미지URL)

예시:
![OpenAI 로고](https://example.com/openai-logo.png)
```

### 이미지에 링크 걸기

이미지를 클릭하면 다른 페이지로 이동하게 만들 수 있다.

```markdown
[![대체텍스트](이미지URL)](링크URL)
```

### GitHub에서 이미지 올리는 가장 쉬운 방법

1. GitHub Issue나 Pull Request 작성 화면을 연다
2. 이미지 파일을 **드래그 앤 드롭**으로 텍스트 영역에 놓는다
3. 자동으로 `![image](https://user-images...)` 형태의 마크다운이 생성된다
4. 이 마크다운을 복사하여 원하는 `.md` 파일에 붙여넣는다

---

## 7. 코드 (Code)

### 인라인 코드

문장 중간에 코드나 명령어를 넣을 때 백틱(`` ` ``)으로 감싼다.

```markdown
파이썬에서 출력은 `print("hello")` 함수를 사용한다.
Temperature는 `0`에서 `2` 사이의 값을 설정할 수 있다.
```

**렌더링 결과**: 파이썬에서 출력은 `print("hello")` 함수를 사용한다.

### 코드 블록

백틱 3개(`` ``` ``)로 감싸면 코드 블록이 된다. 첫 줄에 언어를 명시하면 **문법 하이라이팅**이 적용된다.

````markdown
```python
def greet(name):
    return f"안녕하세요, {name}님!"

print(greet("학생"))
```
````

**렌더링 결과**:

```python
def greet(name):
    return f"안녕하세요, {name}님!"

print(greet("학생"))
```

### 지원하는 언어 예시

| 언어 표기 | 언어 |
|----------|------|
| `python` | Python |
| `javascript` 또는 `js` | JavaScript |
| `bash` 또는 `shell` | Bash/Shell |
| `json` | JSON |
| `markdown` 또는 `md` | Markdown |
| `r` | R |
| `sql` | SQL |
| `html` | HTML |
| `css` | CSS |

### diff 코드 블록 (변경사항 표시)

```markdown
```diff
- 이 줄은 삭제된 내용이다 (빨간색)
+ 이 줄은 추가된 내용이다 (초록색)
  이 줄은 변경되지 않은 내용이다
```　
```

**렌더링 결과**:

```diff
- 이 줄은 삭제된 내용이다 (빨간색)
+ 이 줄은 추가된 내용이다 (초록색)
  이 줄은 변경되지 않은 내용이다
```

---

## 8. 인용문 (Blockquotes)

`>` 기호를 줄 앞에 붙이면 인용문이 된다.

```markdown
> 이것은 인용문이다.
> 여러 줄에 걸쳐 작성할 수 있다.

> 중첩 인용도 가능하다.
>> 이것은 인용 안의 인용이다.
>>> 3단계 중첩도 된다.
```

**렌더링 결과**:

> 이것은 인용문이다.
> 여러 줄에 걸쳐 작성할 수 있다.

> 중첩 인용도 가능하다.
>> 이것은 인용 안의 인용이다.

### 인용문 + 강조 조합

```markdown
> ⚠️ **주의**: Temperature를 2 이상으로 설정하면 출력이 불안정해질 수 있다.

>  **팁**: 프롬프트를 테스트할 때에는 Temperature를 `0`으로 설정하여 
> 일관성을 먼저 확인한 후, 점차 올려가며 조절하는 것이 권장된다.
```

**렌더링 결과**:

>  **주의**: Temperature를 2 이상으로 설정하면 출력이 불안정해질 수 있다.

>  **팁**: 프롬프트를 테스트할 때에는 Temperature를 `0`으로 설정하여 일관성을 먼저 확인한 후, 점차 올려가며 조절하는 것이 권장된다.

---

## 9. 표 (Tables)

파이프(`|`)와 하이픈(`-`)을 조합하여 표를 만든다.

### 기본 표

```markdown
| 이름 | 나이 | 전공 |
|------|------|------|
| 김철수 | 22 | 컴퓨터공학 |
| 이영희 | 21 | 경영학 |
| 박민수 | 23 | 디자인 |
```

**렌더링 결과**:

| 이름 | 나이 | 전공 |
|------|------|------|
| 김철수 | 22 | 컴퓨터공학 |
| 이영희 | 21 | 경영학 |
| 박민수 | 23 | 디자인 |

### 정렬

헤더 구분선에 콜론(`:`)을 추가하여 정렬을 지정한다.

```markdown
| 왼쪽 정렬 | 가운데 정렬 | 오른쪽 정렬 |
|:----------|:----------:|-----------:|
| Left | Center | Right |
| 텍스트 | 텍스트 | 텍스트 |
```

**렌더링 결과**:

| 왼쪽 정렬 | 가운데 정렬 | 오른쪽 정렬 |
|:----------|:----------:|-----------:|
| Left | Center | Right |
| 텍스트 | 텍스트 | 텍스트 |

>  **팁**: 표를 직접 타이핑하는 것이 번거롭다면, [Tables Generator](https://www.tablesgenerator.com/markdown_tables) 같은 온라인 도구를 활용하면 편리하다.

---

## 10. 수평선 (Horizontal Rules)

세 개 이상의 하이픈(`---`), 별표(`***`), 또는 밑줄(`___`)을 입력하면 수평선이 생성된다.

```markdown
---
```

**렌더링 결과**:

---

>  **활용**: 문서의 큰 섹션을 구분할 때 사용한다. 이 문서에서도 각 섹션 사이에 `---`를 사용하고 있다.

---

## 11. 체크리스트 (Task Lists)

GitHub에서 특히 유용한 기능이다. Issue나 Pull Request에서 진행 상황을 추적할 때 자주 사용한다.

```markdown
- [x] 프롬프트 기초 개념 학습 완료
- [x] OpenAI Playground 계정 생성
- [ ] Temperature 실습 수행
- [ ] Few-Shot 프롬프트 작성 연습
- [ ] 최종 프로젝트 제출
```

**렌더링 결과**:

- [x] 프롬프트 기초 개념 학습 완료
- [x] OpenAI Playground 계정 생성
- [ ] Temperature 실습 수행
- [ ] Few-Shot 프롬프트 작성 연습
- [ ] 최종 프로젝트 제출

>  GitHub에서는 렌더링된 체크박스를 **직접 클릭**하여 체크/해제할 수 있다.

---

## 12. 접기/펼치기 (Collapsible Sections)

긴 내용을 접어두고 필요할 때만 펼쳐 볼 수 있게 하는 기능이다. HTML의 `<details>` 태그를 사용한다.

```markdown
<details>
<summary>클릭하면 펼쳐지는 영역</summary>

여기에 숨겨진 내용을 작성한다.

- 목록도 사용할 수 있다
- **강조**도 가능하다

코드 블록도 넣을 수 있다.

</details>
```

**렌더링 결과**:

<details>
<summary>클릭하면 펼쳐지는 영역</summary>

여기에 숨겨진 내용을 작성한다.

- 목록도 사용할 수 있다
- **강조**도 가능하다

</details>

>  **활용 예시**: FAQ, 긴 코드 예시, 부가 설명, 정답 공개 등에 유용하다.

---

## 13. 이모지 (Emoji)

GitHub 마크다운에서는 `:이모지이름:` 형식으로 이모지를 삽입할 수 있다.

```markdown
:rocket: 로켓     :star: 별     :bulb: 전구
:warning: 경고    :memo: 메모    :tada: 축하
:white_check_mark: 체크   :x: 엑스   :fire: 불
```

**렌더링 결과**: :rocket: :star: :bulb: :warning: :memo: :tada:

자주 쓰는 이모지 모음:

| 코드 | 이모지 | 용도 |
|------|--------|------|
| `:bulb:` | 💡 | 팁, 아이디어 |
| `:warning:` | ⚠️ | 주의사항 |
| `:memo:` | 📝 | 메모, 기록 |
| `:rocket:` | 🚀 | 출시, 시작 |
| `:white_check_mark:` | ✅ | 완료 |
| `:x:` | ❌ | 금지, 실패 |
| `:star:` | ⭐ | 중요, 즐겨찾기 |

> 💡 또는 운영체제의 이모지 입력 기능을 사용해도 된다 (Mac: `Ctrl+Cmd+Space`, Windows: `Win+.`).

---

## 14. 각주 (Footnotes)

```markdown
프롬프트 엔지니어링[^1]은 AI 모델의 출력을 최적화하는 과정이다.

LLM[^2]의 성능은 모델 파라미터뿐 아니라 프롬프트 설계에 크게 의존한다.

[^1]: Prompt Engineering. AI 모델로부터 원하는 결과물을 얻기 위해 프롬프트를 설계하고 개선하는 과정.
[^2]: Large Language Model. 대규모 언어 모델.
```

> ⚠️ 각주는 GitHub에서 지원되지만, 모든 마크다운 렌더러에서 지원되는 것은 아니다.

---

## 15. 수식 (Math) — GitHub 지원

GitHub는 LaTeX 문법의 수식 렌더링을 지원한다.

### 인라인 수식

```markdown
아인슈타인의 에너지 공식은 $E = mc^2$ 이다.
```

**렌더링 결과**: 아인슈타인의 에너지 공식은 $E = mc^2$ 이다.

### 블록 수식

```markdown
$$
\frac{\partial L}{\partial \theta} = \sum_{i=1}^{N} \nabla_\theta \log p_\theta(y_i | x_i)
$$
```

**렌더링 결과**:

$$
\frac{\partial L}{\partial \theta} = \sum_{i=1}^{N} \nabla_\theta \log p_\theta(y_i | x_i)
$$

---

## 16. 프롬프트 엔지니어링에서의 마크다운 활용

강의 자료에서 다루었듯이, 마크다운은 프롬프트를 구조화하는 핵심 기법이다. AI 모델이 텍스트를 더 쉽게 처리할 수 있도록, 사람의 언어가 아닌 모델이 읽기 쉬운 구조로 프롬프트를 구성할 때 마크다운을 사용한다.

### 예시: 구조화되지 않은 프롬프트 vs 마크다운 구조화 프롬프트

**구조화 전 (자연어)**:

```
나는 감정 분석을 하고 싶어. 텍스트를 주면 긍정인지 부정인지 중립인지 분류해줘.
그리고 결과는 JSON 형태로 줬으면 좋겠어. 텍스트는 "이 영화 정말 재밌었어"야.
```

**구조화 후 (마크다운)**:

```markdown
### 지시
아래 텍스트의 감정을 분류하라.

### 분류 기준
- 긍정 (Positive)
- 부정 (Negative)  
- 중립 (Neutral)

### 입력 텍스트
이 영화 정말 재밌었어

### 출력 형식
JSON 형태로 출력하라:
{"text": "...", "sentiment": "...", "confidence": 0.0~1.0}
```

두 프롬프트 모두 같은 작업을 지시하지만, 마크다운으로 구조화한 버전이 더 정확하고 일관된 결과를 산출한다. `###`으로 섹션을 구분하고, `-`로 항목을 나열하면 AI가 각 요소를 명확히 구분하여 처리할 수 있기 때문이다.

---

## 17. 실전 종합 예제: README.md 작성하기

GitHub 저장소에 올리는 대표 문서인 `README.md`의 실전 템플릿을 제공한다.

````markdown
#  프롬프트 엔지니어링 실습 프로젝트

> 2025학년도 1학기 프롬프트 엔지니어링 강의 실습 저장소

##  프로젝트 소개

이 저장소는 프롬프트 엔지니어링 기법을 실습하고
결과를 기록하기 위한 공간이다.

##  사용 기술

- **모델**: GPT-4o-mini, GPT-4o
- **프레임워크**: LangChain, LangGraph
- **언어**: Python 3.11+

##  폴더 구조

```
├── week01_basics/        # 프롬프트 기초
├── week02_zero_few_shot/ # Zero-Shot & Few-Shot
├── week03_cot/           # Chain-of-Thought
├── week04_agent/         # AI 에이전트
└── README.md
```

##  시작하기

1. 이 저장소를 클론한다
   ```bash
   git clone https://github.com/username/prompt-engineering-lab.git
   ```
2. 의존성을 설치한다
   ```bash
   pip install -r requirements.txt
   ```
3. API 키를 설정한다
   ```bash
   export OPENAI_API_KEY="your-key-here"
   ```

##  주차별 학습 기록

| 주차 | 주제 | 상태 |
|------|------|------|
| 1주차 | 프롬프트 기초와 설계 원칙 | ✅ 완료 |
| 2주차 | Zero-Shot & Few-Shot | ✅ 완료 |
| 3주차 | Chain-of-Thought | 🔄 진행 중 |
| 4주차 | AI 에이전트 제작 | ⬜ 예정 |

##  참고 자료

- [OpenAI Playground 가이드](./docs/OpenAI_Playground_Guide.md)
- [마크다운 문법 가이드](./docs/Markdown_Guide.md)
- [LangChain 한국어 튜토리얼](https://github.com/teddylee777/langchain-kr)

##  라이선스

이 프로젝트는 MIT 라이선스를 따른다.
````

---

## 18. 자주 하는 실수와 해결법

### 실수 1: 줄바꿈이 안 된다

```markdown
❌ 이렇게 쓰면
줄바꿈이 되지 않는다.

✅ 해결법 1: 줄 끝에 공백 2칸을 넣는다
첫 번째 줄  ← (여기에 스페이스 2개)
두 번째 줄

✅ 해결법 2: 빈 줄을 하나 넣는다
첫 번째 문단

두 번째 문단
```

### 실수 2: 제목이 렌더링되지 않는다

```markdown
❌ #제목    (# 뒤에 공백이 없음)
✅ # 제목   (# 뒤에 공백 1칸 필요)
```

### 실수 3: 목록이 중첩되지 않는다

```markdown
❌ 들여쓰기가 부족:
- 상위
- 하위 (같은 레벨로 인식됨)

✅ 스페이스 2칸 들여쓰기:
- 상위
  - 하위 (중첩으로 인식됨)
```

### 실수 4: 표가 깨진다

```markdown
❌ 헤더 구분선이 없음:
| 이름 | 점수 |
| 철수 | 90 |

✅ 헤더 아래에 구분선 필수:
| 이름 | 점수 |
|------|------|
| 철수 | 90 |
```

### 실수 5: 코드 블록 안에서 백틱을 사용하고 싶다

````markdown
❌ 백틱 3개 안에 백틱 3개:
```
이 안에 ```를 넣으면 깨진다
```

✅ 바깥쪽을 백틱 4개로 감싼다:
````
이 안에 ```를 넣어도 안전하다
```
````　
````

---

## 19. 치트시트 (한 장 요약)

```
# 제목               → H1 제목 (문서당 1개)
## 소제목             → H2 소제목
### 소소제목          → H3 소소제목

**굵게**             → 볼드체
*기울임*             → 이탤릭체
~~취소선~~           → 취소선
`인라인 코드`         → 인라인 코드

- 항목               → 순서 없는 목록
1. 항목              → 순서 있는 목록
- [x] 완료           → 체크리스트 (체크됨)
- [ ] 미완료         → 체크리스트 (미체크)

[텍스트](URL)        → 하이퍼링크
![대체텍스트](URL)    → 이미지

> 인용문             → 인용 블록

| A | B |            → 표 (헤더)
|---|---|            → 표 (구분선, 필수)
| 1 | 2 |            → 표 (데이터)

---                  → 수평선

```언어명             → 코드 블록 시작
```                  → 코드 블록 끝

$수식$              → 인라인 수식
$$수식$$            → 블록 수식
```

---

> 내용에 대한 질문이나 오류 제보는 Issues 탭을 통해 남겨주시기 바란다.

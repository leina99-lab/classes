# 🎮 OpenAI Playground 완전 정복 가이드


> 이 문서는 OpenAI Playground를 처음 사용하는 학생을 위한 단계별 안내서이다.  
> 코딩 경험이 전혀 없어도 따라할 수 있도록 구성하였다.

---

## 📖 목차

1. [OpenAI Playground란 무엇인가](#1-openai-playground란-무엇인가)
2. [ChatGPT와 Playground의 차이](#2-chatgpt와-playground의-차이)
3. [사전 준비: 계정 만들기와 크레딧 충전](#3-사전-준비-계정-만들기와-크레딧-충전)
4. [Playground 화면 구성 완전 해부](#4-playground-화면-구성-완전-해부)
5. [3가지 모드 이해하기: Chat / Assistants / Completions](#5-3가지-모드-이해하기)
6. [6가지 생성 파라미터 상세 설명](#6-6가지-생성-파라미터-상세-설명)
7. [실습 1: 첫 번째 프롬프트 실행하기](#7-실습-1-첫-번째-프롬프트-실행하기)
8. [실습 2: Temperature 차이를 직접 체감하기](#8-실습-2-temperature-차이를-직접-체감하기)
9. [실습 3: 시스템 프롬프트로 AI의 역할 지정하기](#9-실습-3-시스템-프롬프트로-ai의-역할-지정하기)
10. [실습 4: Compare 기능으로 모델 비교하기](#10-실습-4-compare-기능으로-모델-비교하기)
11. [실습 5: Few-Shot 프롬프트 테스트하기](#11-실습-5-few-shot-프롬프트-테스트하기)
12. [비용 관리 팁](#12-비용-관리-팁)
13. [자주 묻는 질문 (FAQ)](#13-자주-묻는-질문-faq)
14. [참고 자료](#14-참고-자료)

---

## 1. OpenAI Playground란 무엇인가

OpenAI Playground는 OpenAI가 제공하는 **웹 기반 프롬프트 테스트 환경**이다. 쉽게 비유하면, ChatGPT가 "완성된 제품"이라면 Playground는 **"실험실"**에 해당한다.

이 실험실에서 할 수 있는 일은 다음과 같다.

- 다양한 AI 모델(GPT-4o, GPT-4o-mini, GPT-3.5 등)을 **직접 선택**하여 테스트할 수 있다
- **시스템 프롬프트**를 설정하여 AI의 역할과 행동 규칙을 정의할 수 있다
- **Temperature, Top P** 등 생성 조건을 슬라이더로 조절하며 결과의 변화를 관찰할 수 있다
- 동일한 프롬프트를 **서로 다른 설정**으로 나란히 비교할 수 있다
- 만족스러운 설정 조합을 **프리셋(Preset)**으로 저장하여 재사용할 수 있다

> 💡 **한 줄 정리**: Playground는 프롬프트 엔지니어링을 연습하고 검증하기 위한 최적의 도구이다.

---

## 2. ChatGPT와 Playground의 차이

"ChatGPT를 쓰면 되지, 왜 Playground를 따로 써야 하나요?"라는 질문을 자주 받는다. 아래 표를 통해 차이를 확인하자.

| 구분 | ChatGPT (chat.openai.com) | Playground (platform.openai.com/playground) |
|------|--------------------------|---------------------------------------------|
| **대상** | 일반 사용자 | 개발자, 프롬프트 엔지니어, 학습자 |
| **모델 선택** | 자동 (Plus 사용자는 일부 선택 가능) | GPT-4o, GPT-4o-mini, GPT-3.5 등 직접 선택 |
| **시스템 프롬프트** | 설정 불가 (Custom GPT 제외) | 자유롭게 작성 가능 |
| **파라미터 조절** | 불가능 | Temperature, Top P, Max Tokens 등 6가지 조절 가능 |
| **비용** | 월 구독료 ($20/월 for Plus) | 사용한 토큰만큼 과금 (종량제) |
| **결과 비교** | 불가능 | Compare 기능으로 나란히 비교 가능 |
| **코드 내보내기** | 불가능 | `</>` 버튼으로 Python/curl 코드 자동 생성 |

> 💡 **핵심 차이**: ChatGPT는 "대화"에 초점을 맞추고, Playground는 "실험과 제어"에 초점을 맞춘다.

---

## 3. 사전 준비: 계정 만들기와 크레딧 충전

### Step 1. OpenAI Platform 계정 생성

1. 브라우저에서 **[platform.openai.com](https://platform.openai.com)** 에 접속한다
2. **Sign up** 버튼을 클릭한다
3. 이메일, Google, Microsoft, Apple 계정 중 하나로 가입한다
4. 이메일 인증을 완료한다
5. 이름과 생년월일을 입력하고 이용 약관에 동의한다

> ⚠️ **주의**: ChatGPT 계정(chat.openai.com)과 Platform 계정(platform.openai.com)은 **같은 로그인 정보**를 사용하지만, **별도의 결제 체계**를 갖고 있다. ChatGPT Plus 구독과 API 크레딧은 별개이다.

### Step 2. API 크레딧 충전

1. 로그인 후 좌측 메뉴에서 **Settings** → **Billing** 으로 이동한다
2. **Add payment method** 를 클릭하여 해외결제 가능한 신용카드를 등록한다
3. **Add to credit balance** 에서 원하는 금액을 충전한다 (최소 $5부터 가능)

> 💰 **비용 참고**: GPT-4o-mini 모델 기준으로 $5 크레딧이면 약 200만 토큰(대략 한국어 50만 자 정도)을 처리할 수 있다. 수업 실습 용도로는 $5~10이면 충분하다.

### Step 3. API Key 발급 (선택사항)

Playground만 사용할 경우 API Key가 반드시 필요하지는 않지만, 나중에 Python 코드로 연동할 때 필요하므로 미리 발급해 두면 편리하다.

1. 좌측 메뉴에서 **API keys** 를 클릭한다
2. **Create new secret key** 를 클릭한다
3. 생성된 키를 **즉시 복사하여 안전한 곳에 저장**한다 (이후 다시 볼 수 없다)

> 🔐 **보안 주의**: API Key는 절대 다른 사람에게 공유하거나 GitHub에 업로드하면 안 된다. 타인이 이 키를 사용하면 본인의 크레딧이 소진된다.

---

## 4. Playground 화면 구성 완전 해부

Playground에 접속하면(**platform.openai.com/playground**) 세 개의 영역으로 나뉜 화면이 나타난다.

```
┌─────────────┬──────────────────────────────────┬──────────────────┐
│             │                                  │                  │
│  좌측 메뉴   │         중앙 대화 영역              │  우측 설정 패널   │
│             │                                  │                  │
│  • Chat     │  ┌─────────────────────────────┐  │  Temperature: 1  │
│  • Assistants│  │ SYSTEM                      │  │  Max Tokens: 256 │
│  • Completions│ │ Enter system instructions   │  │  Stop sequences  │
│             │  └─────────────────────────────┘  │  Top P: 1        │
│  • Threads  │                                  │  Freq penalty: 0 │
│  • Fine-tuning│                                │  Pres penalty: 0 │
│  • Batches  │  ┌─────────────────────────────┐  │                  │
│  • Storage  │  │ Enter user message...       │  │                  │
│  • Usage    │  │              [User] [Run ⌘↵] │  │                  │
│  • API keys │  └─────────────────────────────┘  │                  │
└─────────────┴──────────────────────────────────┴──────────────────┘
```

### 좌측 메뉴

| 메뉴 항목 | 역할 |
|----------|------|
| **Chat** | 대화형 프롬프트를 테스트하는 메인 모드 (가장 많이 사용) |
| **Assistants** | 파일 업로드, 코드 실행 등 고급 기능을 갖춘 어시스턴트 모드 |
| **Completions** | 레거시(구형) 모드. 문장 완성 방식으로 작동 (2025년 서비스 종료 예정) |
| **Usage** | API 사용량과 비용을 확인하는 대시보드 |
| **API keys** | API 키를 생성하고 관리하는 페이지 |

### 중앙 대화 영역

- **상단 모델 선택 드롭다운**: 사용할 모델을 선택한다 (예: `gpt-4o`, `gpt-4o-mini`)
- **SYSTEM 영역**: 시스템 프롬프트를 입력하는 공간이다. AI의 역할, 행동 규칙, 제약 조건 등을 여기에 기술한다
- **하단 메시지 입력창**: 사용자 프롬프트를 입력하는 공간이다
- **[User] 버튼**: 메시지의 역할(User/Assistant)을 전환할 수 있다
- **[Run ⌘↵] 버튼**: 프롬프트를 실행한다 (Mac: `Cmd+Enter`, Windows: `Ctrl+Enter`)
- **Compare ✦ 버튼**: 두 가지 설정의 결과를 나란히 비교하는 모드를 활성화한다

### 우측 설정 패널

6가지 생성 파라미터가 슬라이더와 입력 필드로 제공된다. 각 파라미터의 상세 설명은 [섹션 6](#6-6가지-생성-파라미터-상세-설명)에서 다룬다.

### 상단 도구 모음

| 버튼 | 기능 |
|------|------|
| **Presets** | 저장된 프리셋을 불러오거나 현재 설정을 프리셋으로 저장한다 |
| **Save** | 현재 설정을 프리셋으로 저장한다 |
| **⏱ (History)** | 이전에 실행한 프롬프트 기록을 조회한다 |
| **`</>`** | 현재 설정을 Python, curl, Node.js 코드로 자동 변환한다 |

---

## 5. 3가지 모드 이해하기

### 🗨️ Chat 모드 (권장)

**가장 많이 사용하는 모드**이다. ChatGPT처럼 대화 형식으로 프롬프트를 테스트한다.

- **시스템 프롬프트 + 사용자 메시지** 구조를 사용한다
- GPT-4o, GPT-4o-mini 등 최신 모델을 지원한다
- 멀티턴(다중 대화) 테스트가 가능하다

> 🎯 **수업에서는 이 모드만 사용하면 된다.**

### 🤖 Assistants 모드

Chat 모드보다 고급 기능을 제공하는 모드이다.

- 파일을 업로드하여 AI가 참조하게 할 수 있다
- Code Interpreter를 활성화하여 AI가 직접 코드를 실행할 수 있다
- Function Calling을 테스트할 수 있다

> 이 모드는 고급 과정에서 다루므로, 지금은 알아만 두면 된다.

### 📝 Completions 모드 (레거시)

OpenAI가 처음 제공한 방식으로, **채팅이 아닌 "문장 완성"** 방식으로 작동한다.

- `babbage-002`, `davinci-002`, `gpt-3.5-turbo-instruct` 모델만 지원한다
- 문장을 입력하면 모델이 그 문장을 이어서 완성하려고 시도한다
- 시스템 프롬프트가 없으며, 하나의 텍스트 상자만 존재한다

> ⚠️ **주의**: 이 모드는 레거시(구형)이며 2025년에 서비스가 종료될 예정이다. 새로 학습하는 학생은 Chat 모드를 사용하면 된다.

---

## 6. 6가지 생성 파라미터 상세 설명

Playground 우측 패널에 있는 6가지 파라미터는 프롬프트 엔지니어링에서 **가장 핵심적인 조절 장치**이다. 각각의 의미와 권장 설정값을 상세히 설명한다.

---

### 🌡️ 파라미터 1: Temperature (온도)

| 항목 | 내용 |
|------|------|
| **범위** | 0 ~ 2 |
| **기본값** | 1 |
| **한 줄 정의** | 모델 출력의 "다양성"을 조절하는 다이얼이다 |

**원리**를 비유로 설명하면 다음과 같다. AI가 다음 단어를 선택할 때, 후보 단어들의 "확률 분포" 중에서 고르게 된다. Temperature가 낮으면 확률이 가장 높은 단어만 선택하여(=보수적), Temperature가 높으면 확률이 낮은 단어도 선택할 수 있게 되어(=모험적) 결과가 다양해진다.

**설정값에 따른 차이**:

```
동일 프롬프트: "한국의 여름 거리를 묘사해줘."

🧊 Temperature 0.0 (매우 낮음):
   → "한국의 여름 거리는 무덥고 습합니다. 사람들은 반팔과 
      반바지를 입고 다니며, 에어컨이 켜진 카페에서 시원한 
      음료를 마시는 모습이 자주 보입니다."
   → 매번 실행해도 거의 동일한 결과가 나온다

🔥 Temperature 1.0 (높음):
   → "찌는 듯한 열기가 아스팔트 위로 아지랑이를 피워올리고, 
      골목 어디선가 수박 파는 트럭의 확성기 소리가 울려 퍼진다. 
      한 여름밤의 포장마차에서는 맥주잔이 딸깍이며 부딪힌다."
   → 매번 실행할 때마다 다른 표현이 나온다
```

**용도별 권장 설정**:

| Temperature | 적합한 작업 | 이유 |
|:-----------:|-----------|------|
| **0.0 ~ 0.3** | 분류, 감정 분석, 코드 생성, 요약, 번역 | 정확성과 일관성이 중요하기 때문 |
| **0.4 ~ 0.7** | 일반 질의응답, 보고서 작성, 설명 | 적당한 정확성과 자연스러움의 균형 |
| **0.8 ~ 1.2** | 브레인스토밍, 스토리텔링, 광고 카피 | 창의성과 다양성이 중요하기 때문 |

> ⚠️ **주의**: Temperature 1.5 이상은 출력이 너무 무작위적으로 변하여 실용성이 떨어진다. 특별한 이유가 없다면 0~1.2 범위 안에서 사용하는 것이 권장된다.

---

### 🎯 파라미터 2: Top P (상위 P, 핵 샘플링)

| 항목 | 내용 |
|------|------|
| **범위** | 0 ~ 1 |
| **기본값** | 1 |
| **한 줄 정의** | 후보 단어의 "선택 범위"를 제한하는 필터이다 |

**원리**를 비유로 설명하면 다음과 같다. AI가 다음 단어를 고를 때 100명의 후보가 있다고 하자. Top P = 0.1이면 "상위 10%에 해당하는 후보"만 남기고 나머지는 제외한다. Top P = 1.0이면 100명 전원이 후보에 남는다.

```
예시: "오늘 날씨가 ___"

Top P = 0.1 → 후보: [좋다, 나쁘다] (최상위 확률 단어만)
Top P = 0.9 → 후보: [좋다, 나쁘다, 흐리다, 화창하다, 쌀쌀하다, ...]
```

> 💡 **실전 팁**: Temperature와 Top P는 **둘 다 "다양성"을 조절**하는 파라미터이다. 따라서 **하나만 조절하고 다른 하나는 기본값에 가깝게 유지**하는 것이 일반적이다. 둘 다 극단적으로 설정하면 예측 불가능한 결과가 나올 수 있다.

---

### 📏 파라미터 3: Maximum Length (최대 토큰 수)

| 항목 | 내용 |
|------|------|
| **범위** | 1 ~ 모델별 상한 (GPT-4o: 최대 16,384) |
| **기본값** | 256 |
| **한 줄 정의** | AI가 생성할 수 있는 응답의 **최대 길이**를 제한하는 설정이다 |

**토큰이란?** AI가 텍스트를 처리하는 최소 단위이다. 영어는 대략 1단어 ≈ 1토큰, 한국어는 1글자 ≈ 1~2토큰 정도로 계산하면 된다.

```
예시:
"안녕하세요"     →  약 3~4 토큰
"프롬프트 엔지니어링"  →  약 6~8 토큰
```

**설정값 가이드**:

| Maximum Length | 용도 |
|:--------------:|------|
| 50~100 | 한 줄짜리 분류 결과, 감정 레이블 |
| 256 (기본) | 짧은 답변, 요약 |
| 500~1000 | 일반적인 설명, 보고서 단락 |
| 2000~4096 | 긴 글, 에세이, 코드 생성 |

> ⚠️ **주의**: 이 값이 낮으면 답변이 **중간에 갑자기 잘릴 수 있다**. "답변이 갑자기 끊겼어요!"라는 문제가 발생하면 이 값을 높여보자. 다만, 토큰 수 = 비용이므로 필요한 만큼만 설정하는 것이 경제적이다.

---

### 🔄 파라미터 4: Frequency Penalty (빈도 패널티)

| 항목 | 내용 |
|------|------|
| **범위** | 0 ~ 2 |
| **기본값** | 0 |
| **한 줄 정의** | 이미 나온 단어가 **다시 나오는 것**을 억제하는 설정이다 |

**원리**: 모델이 이전에 선택한 토큰이나 패턴과 비슷한 토큰을 다시 선택하는 것을 억제한다. **나온 횟수에 비례**하여 패널티를 부과한다.

```
예시: Frequency Penalty = 0 (기본)
→ "사과는 맛있는 과일이다. 사과는 빨간색이다. 사과는 건강에 좋다."
   (같은 단어 "사과는"이 반복됨)

예시: Frequency Penalty = 1.5
→ "사과는 맛있는 과일이다. 이 과일은 빨간색을 띠며, 건강에도 이롭다."
   (다양한 표현으로 대체됨)
```

> 💡 **언제 올릴까?**: 모델이 같은 문장이나 단어를 계속 반복할 때 0.5~1.0 정도로 올려보자.

---

### 🆕 파라미터 5: Presence Penalty (존재 패널티)

| 항목 | 내용 |
|------|------|
| **범위** | 0 ~ 2 |
| **기본값** | 0 |
| **한 줄 정의** | **새로운 주제**로 이동하도록 유도하는 설정이다 |

**Frequency Penalty와의 차이**:

| 구분 | Frequency Penalty | Presence Penalty |
|------|-------------------|-----------------|
| 패널티 기준 | 단어가 나온 **횟수**에 비례 | 단어가 **한 번이라도** 나왔는지 여부 |
| 효과 | 같은 단어의 반복을 줄임 | 새로운 단어/주제의 도입을 촉진 |
| 비유 | "방금 말한 거 또 말하지 마" | "새로운 얘기도 해봐" |

> 💡 **언제 올릴까?**: AI가 같은 주제만 맴돌고 새로운 관점을 제시하지 않을 때 올려보자.

---

### 🛑 파라미터 6: Stop Sequences (중지 시퀀스)

| 항목 | 내용 |
|------|------|
| **기본값** | 없음 |
| **한 줄 정의** | 특정 문자열이 나오면 **즉시 생성을 멈추게** 하는 설정이다 |

```
예시: Stop Sequence를 "###"으로 설정

프롬프트: "한국의 수도를 알려줘."
→ 모델 출력: "한국의 수도는 서울입니다.###"
   이 시점에서 생성이 즉시 중단됨 (### 이후의 내용은 나오지 않음)
```

**활용 예시**:
- JSON 형식의 출력에서 `}` 를 Stop Sequence로 설정하면 하나의 JSON 객체만 생성하고 멈춘다
- 대화 로그에서 `User:` 를 Stop Sequence로 설정하면 AI가 사용자 역할까지 생성하는 것을 방지한다

---

### 📋 6가지 파라미터 요약 치트시트

| 파라미터 | 범위 | 기본값 | 올리면 | 내리면 |
|---------|------|--------|--------|--------|
| **Temperature** | 0~2 | 1 | 더 창의적, 다양한 출력 | 더 정확, 일관된 출력 |
| **Top P** | 0~1 | 1 | 더 많은 후보 단어 고려 | 더 적은 후보만 고려 |
| **Max Length** | 1~16K | 256 | 더 긴 답변 허용 | 더 짧은 답변으로 제한 |
| **Freq Penalty** | 0~2 | 0 | 단어 반복 억제 | 반복 허용 |
| **Pres Penalty** | 0~2 | 0 | 새 주제 도입 촉진 | 현재 주제 유지 |
| **Stop Seq** | 텍스트 | 없음 | 특정 지점에서 생성 중단 | - |

---

## 7. 실습 1: 첫 번째 프롬프트 실행하기

자, 이제 직접 해보자! 아래 순서를 **그대로** 따라하면 된다.

### 순서

1. **[platform.openai.com/playground](https://platform.openai.com/playground)** 에 접속한다
2. 좌측 메뉴에서 **Chat**이 선택되어 있는지 확인한다
3. 상단 모델 선택 드롭다운에서 **`gpt-4o-mini`** 를 선택한다 (비용이 저렴하다)
4. 우측 패널의 설정은 모두 기본값으로 둔다
5. 하단 메시지 입력창에 아래 프롬프트를 입력한다:

```
다음 텍스트를 긍정, 부정, 중립 중 하나로 분류해.
텍스트: 나는 마라탕 맛이 그저 그랬어.
```

6. **Run** 버튼을 클릭한다 (또는 `Cmd+Enter` / `Ctrl+Enter`)

### 예상 결과

```
중립
```

축하한다! 첫 번째 프롬프트를 성공적으로 실행하였다. 예시도 없이(Zero-Shot) AI가 "분류"라는 맥락을 스스로 이해하고 올바른 답을 제시하였다.

---

## 8. 실습 2: Temperature 차이를 직접 체감하기

### 순서

1. SYSTEM 영역은 비워둔다
2. 메시지 입력창에 다음을 입력한다:

```
한국의 여름 거리를 묘사해줘. 3문장으로.
```

3. **우측 패널에서 Temperature를 `0`으로 설정**한다
4. **Run**을 누른다 → 결과를 메모장에 복사해둔다
5. 대화를 초기화한다 (새 대화 시작)
6. **동일한 프롬프트**를 다시 입력한다
7. **Temperature를 `1.2`로 변경**한다
8. **Run**을 누른다 → 결과를 비교한다

### 관찰 포인트

- Temperature 0일 때: 2~3번 실행해도 **거의 동일한** 결과가 나오는지 확인한다
- Temperature 1.2일 때: 매번 **다른 표현**이 나오는지 확인한다
- 어떤 설정이 "분류 작업"에 적합하고, 어떤 설정이 "창작 작업"에 적합한지 토론해보자

---

## 9. 실습 3: 시스템 프롬프트로 AI의 역할 지정하기

### 순서

1. **SYSTEM 영역**에 다음을 입력한다:

```
당신은 프롬프트 엔지니어링 강의의 조교이다.
학생들의 질문에 친절하고 정확하게 답변한다.
전문 용어를 사용할 때에는 반드시 쉬운 설명을 덧붙인다.
답변은 항상 한국어로 작성한다.
```

2. 메시지 입력창에 다음을 입력한다:

```
Few-Shot Prompting이 뭐예요?
```

3. **Run**을 누른다

4. 이번에는 SYSTEM 영역을 다음으로 **변경**한다:

```
You are a sarcastic AI that answers everything with dry humor.
Respond in English only.
```

5. **동일한 질문**을 다시 입력하고 결과를 비교한다

### 관찰 포인트

- 시스템 프롬프트만 바꾸었을 뿐인데 **답변의 톤, 언어, 스타일이 완전히 달라진다**
- 이것이 바로 시스템 프롬프트의 힘이며, 에이전트의 "캐릭터"를 정의하는 방법이다

---

## 10. 실습 4: Compare 기능으로 모델 비교하기

### 순서

1. 중앙 영역 상단의 **Compare ✦** 버튼을 클릭한다
2. 화면이 **좌우 2개 패널**로 나뉜다
3. 왼쪽 패널: 모델을 **`gpt-4o-mini`**, Temperature를 **0.3**으로 설정한다
4. 오른쪽 패널: 모델을 **`gpt-4o`**, Temperature를 **0.3**으로 설정한다
5. 동일한 프롬프트를 입력한다:

```
양자역학의 불확정성 원리를 초등학생도 이해할 수 있게 비유로 설명해줘.
```

6. **Run**을 누르면 두 모델의 결과가 **나란히** 표시된다

### 관찰 포인트

- 모델 크기에 따라 설명의 **깊이와 비유의 적절성**이 어떻게 달라지는지 비교한다
- 동일한 작업에서 저렴한 모델(gpt-4o-mini)이 충분한 품질을 내는지 평가한다
- 비용 대비 성능의 "경제성" 원칙을 직접 체감할 수 있다

---

## 11. 실습 5: Few-Shot 프롬프트 테스트하기

### 순서

1. Temperature를 **0.3**으로 설정한다
2. SYSTEM 영역은 비워둔다
3. 메시지 입력창에 다음 Few-Shot 프롬프트를 입력한다:

```
이건 정말 굉장해! // 긍정
와 정말 나쁘다! // 부정
오늘 날씨가 좋은 것 같아 // 긍정
그냥 뭐 보통이야 // 중립
아우 정말 끔찍해 //
```

4. **Run**을 누른다

### 예상 결과

```
부정
```

### 관찰 포인트

- 예시(Shot)의 **패턴(`텍스트 // 레이블`)**을 모델이 자동으로 인식하여 동일한 형식으로 응답한다
- "부정"이라고 명시적으로 지시하지 않았는데도 올바른 분류가 이루어진다
- 이것이 **In-context Learning**(문맥 내 학습)이며, Few-Shot Prompting의 핵심 원리이다

---

## 12. 비용 관리 팁

API 크레딧은 종량제로 과금되므로, 실습 시 비용을 의식적으로 관리하여야 한다.

### 모델별 대략적 비용 (2025년 기준)

| 모델 | 입력 비용 (1M 토큰당) | 출력 비용 (1M 토큰당) | 성능 |
|------|---------------------|---------------------|------|
| gpt-4o-mini | ~$0.15 | ~$0.60 | 일반 작업에 충분 |
| gpt-4o | ~$2.50 | ~$10.00 | 복잡한 추론에 우수 |

### 비용 절약 방법

1. **실습에는 `gpt-4o-mini`를 사용한다** — 대부분의 프롬프트 엔지니어링 실습에 충분한 성능을 제공한다
2. **Maximum Length를 필요한 만큼만 설정한다** — 기본값 256이면 대부분의 실습에 충분하다
3. **Usage 페이지를 주기적으로 확인한다** — 좌측 메뉴 > Usage에서 실시간 비용을 모니터링한다
4. **Spending limit을 설정한다** — Settings > Limits에서 월간 사용 한도를 설정하면 예상치 못한 과금을 방지할 수 있다

---

## 13. 자주 묻는 질문 (FAQ)

### Q1. Playground는 무료인가요?

**아니다.** Playground는 API 크레딧을 소비하는 종량제 서비스이다. 하지만 신규 가입 시 소액의 무료 크레딧이 제공되는 경우가 있으며, $5만 충전해도 수업 실습 전체를 충분히 수행할 수 있다.

### Q2. ChatGPT Plus 구독이 있으면 Playground도 무료인가요?

**아니다.** ChatGPT Plus($20/월)와 API 크레딧은 완전히 별개의 결제 체계이다. Playground를 사용하려면 API 크레딧을 별도로 충전하여야 한다.

### Q3. 답변이 갑자기 중간에 잘려요.

**Maximum Length(최대 토큰 수)** 값이 너무 낮기 때문이다. 우측 패널에서 이 값을 500~1000으로 올려보자.

### Q4. 같은 질문을 해도 매번 답이 달라져요.

이것은 정상이다. LLM의 본질적 특성인 **무작위성** 때문이다. 매번 동일한 답변을 원하면 **Temperature를 0**으로 설정하면 된다.

### Q5. SYSTEM 프롬프트에 뭘 써야 하나요?

AI의 **역할(Role)**, **행동 지침(Instructions)**, **제약 조건(Constraints)**을 기술하면 된다. 예시:

```
당신은 한국어 맞춤법 교정 전문가이다.
사용자가 입력한 텍스트의 맞춤법 오류만 수정하여 출력한다.
수정한 부분은 【】 기호로 표시한다.
원문의 의미는 절대 변경하지 않는다.
```

### Q6. `</>` 버튼은 뭔가요?

현재 Playground의 설정(모델, 시스템 프롬프트, 파라미터 등)을 **Python, curl, Node.js 코드로 자동 변환**해주는 기능이다. Playground에서 원하는 결과를 얻은 후, 이 코드를 복사하면 프로그램에서 동일한 설정으로 API를 호출할 수 있다.

### Q7. 법인카드로 결제가 안 돼요.

법인 카드의 **해외 결제가 차단**되어 있을 가능성이 높다. 카드사에 해외 결제 허용을 요청하거나, 개인 카드를 사용하여야 한다.

---

## 14. 참고 자료

### 공식 문서

- [OpenAI Playground](https://platform.openai.com/playground) — 바로 접속
- [OpenAI API Documentation](https://platform.openai.com/docs) — 공식 개발 문서
- [OpenAI Pricing](https://openai.com/pricing) — 최신 모델별 가격표
- [OpenAI Tokenizer](https://platform.openai.com/tokenizer) — 텍스트의 토큰 수를 미리 확인하는 도구

### 추천 학습 자료


- [LangChain 한국어 튜토리얼](https://github.com/teddylee777/langchain-kr) — Playground에서 익힌 프롬프트를 코드로 확장할 때 참고

---


> 내용에 대한 질문이나 오류 제보는 Issues 탭을 통해 남겨주시기 바란다.

# **EDA 시각화 마스터 — 학생 워크북**(Student Workbook)

## 2차시 (3시간) · 분포 · 관계 · 비교 · 인터랙티브 · 고차원

---

> **사용 방법**
>
> 본 워크북은 강의를 들으면서 함께 채워가는 노트이다. 빈칸을 직접 손으로 채우는 행위 자체가 기억의 강력한 단서가 된다. 노트북(`L2_학생배포용_EDA시각화마스터.ipynb`)과 나란히 두고 사용하기를 권한다.
>
> → 모든 코드 빈칸과 개념 빈칸은 강의 종료 후 노트북의 정답 토글로 검증할 수 있다.
> → 강의 직후 빈칸을 다시 한 번 ___덮고___ 백지에 핵심 코드를 재구성해 보라.

---

## **0. 학습 목표**(Learning Objectives)

본 차시를 마치면 학습자는 다음을 수행할 수 있어야 한다.

1. **앤스콤의 사중주**를 통해 ____________(이)가 같아도 패턴이 다를 수 있음을 설명한다.
2. 분포 시각화 도구(`__________`, `__________`, `__________`)를 적절히 선택하여 사용한다.
3. 관계 시각화 도구(`__________`, `__________`, `__________`)로 변수 간 관계를 탐색한다.
4. **색맹 안전 팔레트**(`__________`, `__________`, `__________`)를 모든 그래프에 적용한다.
5. **EDA 5단계 체크리스트**를 통해 모델 학습 전 필수 점검을 수행한다.
6. **PCA**·**t-SNE**로 고차원 데이터를 2D로 압축·시각화한다.
7. 각 도구를 **언제 어떤 상황에서** 선택해야 하는지 의사결정 트리로 판단한다.

---

## **0.1 데이터셋**

| 역할 | 데이터셋 | 용도 |
|---|---|---|
| **Ping**(강사 시연) | __________________________ | __________________________ |
| **Pong**(학생 실습) | __________________________ | __________________________ |

---

## **0.2 환경 설정 — 모든 노트북 첫 셀의 표준**

```python
import os, warnings
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

warnings.filterwarnings('ignore')

# 한글 폰트 + 마이너스 부호
plt.rcParams['axes.unicode_minus'] = ____________

# ★ 색맹 안전 팔레트 (오늘부터의 습관)
sns.set_palette('____________')
sns.set_style('____________')
```

> 왜 모든 노트북의 **첫 셀**에 색맹 안전 팔레트를 두는가?
> → ____________________________________________________________________

---

# **Part 1. 분포 시각화**(Distribution)

## **1.0 앤스콤의 사중주** — 시각화의 제1원칙

다음 표를 채워보라. 네 데이터셋의 통계량은 모두 **____________(이)다**.

| 통계량 | I | II | III | IV |
|---|---|---|---|---|
| mean(x) | __ | __ | __ | __ |
| mean(y) | __ | __ | __ | __ |
| corr(x,y) | __ | __ | __ | __ |

그러나 그래프를 그리면:
- **데이터셋 I**: 교과서적 ____________ 관계
- **데이터셋 II**: 실제로는 ____________ 관계
- **데이터셋 III**: ____________ 1개가 회귀선을 끌어당김
- **데이터셋 IV**: x값이 ____________(이)어서 상관 자체가 무의미

> **앤스콤의 교훈**: ____________________________________________________

> **AI 연결**: ML 모델도 ____________ 값만 보면 속는다. ____________ 곡선과 잔차 플롯을 시각화해야 과적합·____________을 발견할 수 있다.

---

## **1.1 히스토그램 + KDE — 한 변수의 분포**

```python
plt.figure(figsize=(11, 5))
sns.____________(data=airbnb, x='price', bins=____, kde=____)
plt.axvline(airbnb['price'].____________(), color='red', linestyle='--',
            label=f"평균: $...")
plt.axvline(airbnb['price'].____________(), color='blue',
            label=f"중앙값: $...")
plt.legend()
plt.show()
```

| 옵션 | 의미 | 빈칸 |
|---|---|---|
| `bins=` | 막대 ____________ | (예: 50) |
| `kde=True` | ____________ 곡선 추가 | |

> 오른쪽 꼬리가 긴 분포(가격, 소득): 평균이 중앙값보다 ____________(쪽)에 위치한다.

> **AI 연결**: 강한 왜도(\|skew\| > __)인 변수는 `np.____________(x)`(로그 변환) 또는 ____________로 정규화한 뒤 모델에 입력한다.

---

## **1.2 박스플롯 — 그룹 비교의 표준**

### 박스플롯 5수치 채우기

```
   이상치   수염         상자            수염   이상치
     ○ ────┤     ┌──────┬──────┐     ├──── ○
           │     │      │      │     │
       ____________  Q1   __   Q3  ____________
```

- 상자(Box) = ____ ~ ____ = 데이터의 **중간 ____ %**
- 가운데 선 = ____ = ____________
- 수염 바깥 = ____________
- IQR = ____ − ____

### 박스플롯 읽는 순서 3단계

1. **상자 위치**(Q2) → 어느 그룹의 ____________이 가장 높은가?
2. **상자 길이**(IQR) → 어느 그룹의 ____________이 큰가?
3. **이상치(○)** → 초고가/초저가 데이터의 위치

```python
sns.____________(data=airbnb, x='neighbourhood_group', y='price',
                  order=order, palette='____________')
```

> **AI 연결**: 박스플롯의 이상치 기준 `Q1 - 1.5×IQR ~ Q3 + 1.5×IQR`은 ____________ 방법(Tukey's fences)이라 한다. 더 정교한 이상 탐지 알고리즘으로 ____________ Forest, ____________-Class SVM, Autoencoder 기반 방법이 있다.

---

## **1.3 바이올린 플롯 — 박스플롯이 놓치는 것**

박스플롯은 ____________ 분포(봉우리가 2개)를 잡지 못한다. 바이올린 플롯은 분포 모양을 직접 보여주어 이를 드러낸다.

```python
sns.____________(data=df, x='group', y='value',
                  inner='____________',  # 사분위선 표시
                  cut=__)
```

- 넓은 부분 = 그 가격대에 데이터가 ____________
- 봉우리 1개 = ____________ 분포(unimodal)
- 봉우리 2개 = ____________ 분포(bimodal) → 두 그룹으로 나뉨을 시사

> **AI 연결**: 바이올린에서 이봉분포가 발견되면 ____________ 알고리즘(K-means 등) 또는 ____________ 모델(Gaussian Mixture)로 군집을 분리할 수 있다.

---

## **1.4 분포-ML 연결 — 왜도, 로그 변환, 표준화**

| 알고리즘 | 가정 | 위반 시 문제 |
|---|---|---|
| 선형 회귀 | 잔차가 ____________ 분포 | 신뢰구간·____________ 부정확 |
| KNN, SVM | 특성 ____________ 유사 | 큰 스케일 특성이 ____________ 지배 |
| 신경망 | 입력이 ____________ | 학습 속도 급감, ____________ 폭발 |

### 변환 단계 빈칸

```python
# 1. 왜도 확인
from scipy.stats import skew
print(f'왜도: {skew(data):.2f}')

# 2. 로그 변환 (왜도 줄이기)
data_log = np.____________(data)

# 3. 표준화 (평균=0, 표준편차=1)
from sklearn.preprocessing import ____________
data_scaled = ____________().fit_transform(data.reshape(-1, 1))
```

> **AI 연결**: GPT/BERT의 ____________________(Layer Normalization)은 각 층 출력을 평균=0, 분산=1로 정규화한다 — 본질적으로 ____________와 동일하다.

### 자기 진단

- |skew| > __ 이면 변환을 고려한다.
- 박스플롯에서 보이지 않는 ____________ 분포는 바이올린이 잡는다.
- KNN은 ____________와 ____________ 모두에 민감하다.

---

# **Part 2. 관계 시각화**(Relationship)

## **2.1 산점도 — 두 변수 관계의 출발점**

산점도의 점 1개 = ____________________________________________________________

```python
sns.____________(data=df, x='x_var', y='y_var',
                  hue='____________',  # 색상 분리
                  style='____________', # ★ 모양도 분리 (색맹 배려)
                  palette='____________')
```

> 왜 `hue=`와 `style=`을 동시에 사용해야 하는가?
> → ____________________________________________________________________

---

## **2.2 상관 ≠ 인과** — 분석가의 핵심 원칙

**잘못된 표현 (X)**: "광고비를 늘렸기 ____________ 매출이 증가했다."
**올바른 표현 (O)**: "광고비와 매출은 ____________ 관계가 있다."

### 고전 사례: 아이스크림과 익사

- 아이스크림 판매량 ↔ 익사 사고: r ≈ ____________
- 진짜 원인은 ____________________(숨은 변수, lurking variable)

> **인과 추론**(causal inference)을 위해서는 ____________ 실험(A/B 테스트, RCT) 또는 별도 분석 기법이 필요하다.

---

## **2.3 회귀선 + 산점도 — `regplot`**

```python
sns.____________(data=df, x='x', y='y',
                  scatter_kws={'alpha': 0.3},
                  line_kws={'color': '____________'})
```

- 빨간 직선 = ____________ 회귀선(OLS line). 식: `y = ____ x + ____`
- 음영 영역 = ____________ % 신뢰구간

### 상관계수 해석 가이드

| \|r\| | 해석 |
|---|---|
| < ____ | 약한 상관 |
| ____ ~ ____ | 보통 상관 |
| > ____ | 강한 상관 |

---

## **2.4 페어플롯 — 모든 변수 쌍 한눈에**

```python
sns.____________(df, hue='category', palette='colorblind')
```

- 대각선: 각 변수의 ____________ 분포
- 비대각선: 두 변수의 ____________

> 변수가 10개 이상이면 페어플롯이 너무 커진다. 먼저 ____________ 히트맵으로 좁히고 페어플롯을 적용하는 워크플로우가 효율적이다.

---

## **2.5 상관 히트맵 — 다변수 관계 한눈에**

```python
import numpy as np
mask = np.____________(np.ones_like(corr, dtype=bool))  # 상삼각 가림

sns.____________(corr, mask=mask, annot=True, fmt='.2f',
                  cmap='____________',  # 색맹 안전(빨강-파랑)
                  center=0, vmin=-1, vmax=1)
```

### 상관 히트맵의 3가지 위험 신호

| 발견 | 의심 | 조치 |
|---|---|---|
| 타겟과 \|r\| ≈ 1.0 | ____________________ | 그 특성 ____________ |
| 두 특성 간 \|r\| > 0.9 | ____________________ | 하나만 남기거나 ____________ |
| 모든 \|r\| < 0.1 | 선형 관계 부족 | ____________ 모델 고려 |

> **AI 연결**: ____________________(Data Leakage)는 ML 실무에서 가장 위험한 실수이다. Kaggle 상위 솔루션이 무효화된 사례도 다수 있다. 상관 히트맵은 **30초 투자로 30시간을 절약**한다.

---

# **Part 3. 비교 시각화**(Comparison)

## **3.1 카운트플롯 — 클래스 불균형의 첫 발견자**

```python
sns.____________(data=df, x='category',
                  hue='subcategory',
                  palette='colorblind')
```

> 이진 분류에서 0:1 = 95:5가 발견되었다 → ____________________(클래스 불균형) → 대책: ____________ 오버샘플링, `____________='balanced'`, F1·ROC-AUC 지표 사용.

---

## **3.2 막대그래프 — 평균 vs 중앙값**

```python
# (A) 평균 - 이상치에 ____________
sns.barplot(data=df, x='group', y='value',
            estimator=np.____________)

# (B) 중앙값 - 이상치에 ____________
sns.barplot(data=df, x='group', y='value',
            estimator=np.____________)
```

> 가격·소득·매출처럼 오른쪽 꼬리가 긴 분포에서는 ____________(이)가 신뢰성이 높다.

---

## **3.3 EDA 5단계 체크리스트** — ML 파이프라인의 필수 관문

순서를 채우고, 각 단계의 도구·발견하는 문제·대응을 채워보라.

| 순서 | 점검 항목 | 도구 | 발견하는 문제 |
|---|---|---|---|
| ① | ____________ 비율 | `____________()` 막대 | 삭제 vs 대체 |
| ② | 분포 형태 | ____________ + KDE | 왜도, 이봉분포, ____________ 필요 |
| ③ | ____________ 존재 | 박스플롯 | 제거 vs 보존(이상 탐지) |
| ④ | 클래스 ____________ | 카운트플롯 | ____________·class_weight |
| ⑤ | 특성 간 관계 | 상관 ____________ | 특성 선택, 다중공선성, ____________ |

> EDA는 ____________(이)아니라 ML 파이프라인의 ____________(이)다. AutoML도 내부적으로 EDA를 수행한다.

---

# **Part 4. 인터랙티브 · 고차원 시각화**

## **4.1 Plotly Express — 인터랙티브의 표준**

```python
import plotly.express as px

fig = px.____________(df, x='lon', y='lat',
                       color='price',
                       color_continuous_scale='____________',
                       hover_data=['name', 'room_type'])
fig.show()
```

### Plotly의 4가지 강점

1. ____________: 마우스를 올리면 정보 표시
2. ____________: 영역 드래그로 확대
3. ____________: 카메라 아이콘으로 PNG 저장
4. ____________: 클릭으로 그룹 켜고 끄기

> 정적 그래프(Matplotlib/Seaborn)는 ____________·논문에, 인터랙티브 그래프(Plotly)는 ____________·발표에 적합하다.

---

## **4.2 PCA — 고차원 데이터의 2D 압축**

| 기법 | 원리 | 강점 | 약점 |
|---|---|---|---|
| **PCA** | ____________이 최대인 축으로 투영 | 빠름, 해석 가능 | 비선형 못 잡음 |
| **t-SNE** | ____________ 관계 보존 | 군집이 명확 | 느림, 거리 의미 없음 |

```python
from sklearn.____________ import PCA
from sklearn.preprocessing import ____________

X_scaled = ____________().fit_transform(X)
pca = PCA(n_components=__, random_state=42)
X_pca = pca.____________(X_scaled)

print(f'설명 분산: {pca.____________.sum():.1%}')
```

---

## **4.3 t-SNE — 군집 시각화의 표준**

```python
from sklearn.____________ import TSNE
X_tsne = TSNE(n_components=2, perplexity=__).fit_transform(X)
```

> **t-SNE 결과 해석 주의**:
> - 거리의 ____________은 의미가 없다
> - ____________ 관계와 군집 분리만 신뢰하라
> - `____________` 값에 따라 결과가 크게 달라진다(보통 5~50)

> **AI 연결 — 임베딩 시각화**: GPT/BERT의 768차원 임베딩을 t-SNE로 시각화하면 의미가 비슷한 단어들이 ____________(이)에 모여 있다. `king − man + woman ≈ ____________`도 2D에서 확인 가능하다.

### PCA vs t-SNE 선택

| 상황 | 선택 |
|---|---|
| 1000차원 → 50차원 압축 후 모델 입력 | ____________ |
| 100만 고객 군집 시각화 | ____________ |
| 단어 임베딩 의미 군집 확인 | ____________ |

---

## **4.4 시각화의 미래 — XAI**(Explainable AI)

| 시점 | 도구 | 목적 |
|---|---|---|
| 학습 ____ (EDA) | 히스토그램, 박스플롯, 상관 히트맵 | 데이터 ____________ 점검 |
| 학습 ____ | ____________ 곡선, 손실 그래프 | 과적합 진단 |
| 학습 ____ (XAI) | 특성 중요도, ____________, Grad-CAM | 모델 ____________ 설명 |

> ____________________(EU AI Act, 2024)는 고위험 AI에 대해 **설명 가능성을 법적으로 요구**한다.

---

# **마무리**

## **그래프 선택 결정 트리** — 본 차시의 결론

```
질문: 무엇을 보고 싶은가?
│
├─ 한 변수 분포 → ____________ + KDE / boxplot / violin
├─ 두 변수 관계 → ____________ / regplot / heatmap
├─ 그룹 비교    → ____________ / barplot / grouped bar
├─ 변수 10개 이상 → ____________ → 2D 시각화
├─ 군집 구조    → ____________
└─ 발표·대시보드 → ____________
```

## **3가지 황금 규칙** — 외워야 할 것

1. **항상 ____________ 를 먼저 그려라** (앤스콤의 사중주)
2. **`sns.set_palette("____________")`** — 모든 노트북의 첫 셀
3. **상관 ____________ 인과** — "~와 관련 있다" vs "~때문이다"

## **30초 핵심 정리** — 빈칸을 30초 안에 채워보라

> 한 변수 분포 → ____________
>
> 두 변수 관계 → ____________
>
> 그룹 비교 → ____________
>
> 다변수 관계 한 번에 → ____________
>
> 인터랙티브 → ____________
>
> 고차원 압축 → ____________
>
> 군집 시각화 → ____________

---

## **자기 점검 — 주관식**

1. 앤스콤의 사중주가 전달하는 핵심 교훈은?
   → ______________________________________________________________________

2. `sns.boxplot`과 `sns.violinplot`의 결정적 차이는?
   → ______________________________________________________________________

3. 색맹 안전 팔레트 3개를 쓰시오.
   → ______________________________________________________________________

4. 상관 히트맵에서 \|r\| ≈ 1.0이 발견되면 가장 먼저 의심해야 할 것은?
   → ______________________________________________________________________

5. EDA 5단계 체크리스트의 순서는?
   → ______________________________________________________________________

6. PCA와 t-SNE 중 새 데이터에도 동일하게 적용 가능한 것은?
   → ______________________________________________________________________

7. GPT의 토큰 임베딩 품질을 시각적으로 점검하는 표준 도구는?
   → ______________________________________________________________________

8. 클래스 불균형 발견 시 사용하는 대표 기법 3가지는?
   → ______________________________________________________________________

---

## **추가 도전 과제**(선택)

1. **Airbnb FacetGrid 15패널**: `sns.FacetGrid`로 5개 자치구 × 3개 room_type = 15패널에 가격 히스토그램을 그리고, 가장 특이한 분포를 가진 패널을 찾아라.

2. **COVID 시계열 비교**: COVID 데이터에서 5개 국가의 일별 신규 확진자를 `melt`로 long 형태로 변환한 뒤, `sns.lineplot`에 `hue='Country'`로 한 그래프에 모두 그려라.

3. **임베딩 시각화 미니 프로젝트**: scikit-learn의 `digits` 또는 `wine` 데이터를 PCA 2D, t-SNE 2D로 각각 시각화하고, 두 방법의 차이를 1단락으로 서술하라.

---

> **다음 차시 예고**: 시각으로 발견한 패턴을 **수치로 일반화**하는 머신러닝 워크플로우를 다룬다. 데이터 분할 → 전처리 → 학습 → 평가의 전 과정에서 본 차시의 시각화 도구가 어떻게 활용되는지 경험하게 된다.

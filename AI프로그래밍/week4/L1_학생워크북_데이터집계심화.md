# **데이터 집계 심화 — 학생 워크북**(Student Workbook)

##· `groupby` · `crosstab` · `pivot_table` · `stack`/`unstack`/`melt`

---

> **사용 방법**
>
> 본 워크북은 강의를 들으면서 함께 채워가는 노트이다. 빈칸을 직접 손으로 채우는 행위 자체가 기억의 강력한 단서가 된다. 노트북(`L1_학생배포용_데이터집계심화.ipynb`)과 나란히 두고 사용하기를 권한다.
>
> → 모든 코드 빈칸과 개념 빈칸은 강의 종료 후 노트북의 정답 토글로 검증할 수 있다.
> → **학습 효과 극대화 팁**: 강의 직후 빈칸을 다시 한 번 ___덮고___ 백지에 핵심 코드를 재구성해 보라.

---

## **0. 학습 목표**(Learning Objectives)

본 차시를 마치면 학습자는 다음을 수행할 수 있어야 한다.

1. **`__________`**(______________________)을 자유롭게 사용하여 단일·다중 그룹 통계를 계산한다.
2. **`__________`**(______________________)으로 두 범주형 변수 간의 관계를 빈도와 비율로 표현한다.
3. **`__________`**(______________________)을 활용해 다축 요약 분석을 수행한다.
4. **`______`**·**`________`**·**`______`**로 wide↔long 형식을 자유롭게 변환한다.
5. 각 도구를 **언제 어떤 상황에서** 선택해야 하는지 판단할 수 있다.

---

# **Part 1. `groupby` — 그룹별 집계의 모든 것**

## **1.1 Split-Apply-Combine 패러다임**

`groupby`는 다음 세 단계로 작동한다. 빈칸을 채우라.

```
[원본 데이터]
     │
     ▼
  ① ____________ (___________)  : 키 컬럼을 기준으로 데이터를 그룹별로 나눈다.
     │
     ▼
  ② ____________ (___________)  : 각 그룹에 ____________ 함수를 적용한다.
     │
     ▼
  ③ ____________ (___________)  : 결과를 다시 하나의 자료구조로 합친다.
     │
     ▼
[집계 결과]
```

> 이 세 단계를 pandas는 ____________ 줄로 표현한다.

---

## **1.2 단일 그룹 집계의 기본**

```python
# 자치구별 평균 가격
airbnb.groupby(________)[________].mean()
```

| 결과 형태 | 인덱스 | 값 |
|---|---|---|
| Series | ____________ | ____________ |

---

## **1.3 다중 통계 집계 — `agg()`**

```python
airbnb.groupby('neighbourhood_group')['price'].agg([
    ____________,    # 평균
    ____________,    # 중앙값
    ____________,    # 표준편차
    ____________,    # 개수
])
```

### 컬럼별 다른 함수 적용

```python
airbnb.groupby('neighbourhood_group').agg({
    'price':  ____________,
    'reviews': ____________,
    'minimum_nights': ____________,
})
```

### 주요 집계 함수 빈칸

| 통계 | 함수 | 통계 | 함수 |
|---|---|---|---|
| 평균 | ____________ | 표준편차 | ____________ |
| 중앙값 | ____________ | 분산 | ____________ |
| 합계 | ____________ | 결측 제외 개수 | ____________ |
| 최솟값 | ____________ | 분위수 | ____________ |
| 최댓값 | ____________ | 고유값 개수 | ____________ |

---

## **1.4 다중 그룹 집계 — `MultiIndex`**

```python
# 자치구 × 방 유형 평균 가격
airbnb.groupby([________, ________])[________].mean()
```

> 결과의 인덱스는 ____________ 인덱스가 된다. 한 인덱스 레벨씩 풀어내려면 `____________()`을 사용한다.

---

## **1.5 `transform` vs `filter` — 고급 그룹 연산**

| 메서드 | 결과 길이 | 대표 용도 |
|---|---|---|
| `agg()` | 그룹 수만큼 ____________ | 그룹별 통계량 계산 |
| `transform()` | 원본과 ____________ | 그룹별 ____________ · 정규화 · 비율 |
| `filter()` | 일부 행 ____________ | 조건 만족 그룹만 남기기 |

### 그룹별 z-score 표준화 패턴

```python
df['z_price'] = df.groupby(________)[________].transform(
    lambda s: (s - s.____________()) / s.____________()
)
```

> **AI 연결**: BatchNormalization(딥러닝)이 미니배치 단위로 같은 변환을 수행한다. 추천 시스템의 ____________________ 정규화도 동일한 패턴이다.

### 자기 진단

- `agg`는 결과 길이가 ____________(이)다.
- `transform`은 결과 길이가 ____________(이)다.
- 그룹별 z-score는 반드시 ____________을 사용해야 한다.

---

# **Part 2. `crosstab` — 범주형 변수의 교차표**

## **2.1 `crosstab` 기본 — 빈도 교차표**

```python
pd.crosstab(________, ________)
```

> 두 ____________ 변수의 ____________ 횟수를 행렬 형태로 표시한다.

---

## **2.2 `normalize` — 비율로 변환**

| 옵션 | 의미 | 합계 |
|---|---|---|
| `normalize=False` (기본) | 절대 ____________ | (없음) |
| `normalize='all'` | 전체 비율 | ____________ |
| `normalize='index'` | 행별 비율 | 각 ____________ |
| `normalize='columns'` | 열별 비율 | 각 ____________ |

> **실무 가이드**: 절대 빈도와 ____________ 비율을 ____________ 두는 것이 가장 정보량이 풍부하다.

---

## **2.3 값을 추가한 교차표 — `values` + `aggfunc`**

```python
pd.crosstab(
    index=airbnb['neighbourhood_group'],
    columns=airbnb['room_type'],
    values=airbnb[________],
    aggfunc=____________,    # mean, sum, median 등
)
```

> 빈도 대신 임의의 ____________ 통계량을 셀에 채운다. 사실상 `pivot_table`과 거의 동등한 기능이 된다.

---

## **2.4 마진(소계) 추가**

```python
pd.crosstab(..., margins=True, margins_name=________)
```

> margins=True를 켜면 행과 열의 ____________ (또는 평균)이 마지막 행/열에 추가된다.

---

## **2.5 `crosstab` vs `groupby + unstack` 비교**

| 특성 | `crosstab` | `groupby + unstack` |
|---|---|---|
| 결과 형태 | ____________ | ____________ (변환 가능) |
| 기본 통계 | ____________ | 임의 함수 가능 |
| 정규화 | ____________ 옵션 | 직접 계산 |
| 마진 | ____________ | 수동 추가 |
| 코드 길이 | ____________ | 더 길어짐 |

> **결론**: 빈도/비율 표는 ____________________, 복잡한 다중 통계는 ____________________을 사용한다.

---

## **2.6 AI 연결 — 혼동행렬도 `crosstab`이다**

```python
# scikit-learn
from sklearn.metrics import confusion_matrix
confusion_matrix(y_true, y_pred)

# pandas — 동등
pd.crosstab(________, ________)
```

| TP | FN |
|---|---|
| ____________ | ____________ |

| FP | TN |
|---|---|
| ____________ | ____________ |

> 분류 모델의 Accuracy, Precision, Recall, F1은 모두 이 ____________________ 위에서 정의된다. 나이브 베이즈, 카이제곱 독립성 검정 또한 같은 표 위에서 작동한다.

### 자기 진단

- 두 범주형 변수의 빈도표 → ____________
- 두 범주형 변수의 행별 비율 → `crosstab(..., normalize='____________')`
- 분류 모델 평가의 핵심 행렬 → ____________________

---

# **Part 3. `pivot_table` — 다축 요약의 표준**

## **3.1 인자 구조 한눈에**

```python
pd.pivot_table(
    data,                    # ____________
    index=____________,       # 행 차원 (단일 또는 리스트)
    columns=____________,     # 열 차원 (선택)
    values=____________,      # 셀에 들어갈 ____________ 컬럼
    aggfunc=____________,     # 집계 함수 (기본 ____________)
    fill_value=____________,  # 결측 셀 채우기
    margins=____________,     # 합계 행/열 추가
)
```

---

## **3.2 단일 인덱스 / 단일 컬럼**

```python
# 자치구별 방 유형별 평균 가격
pd.pivot_table(
    airbnb,
    index=________,
    columns=________,
    values=________,
    aggfunc=________,
)
```

> 위 코드는 의미상 `pd.crosstab(..., values=..., aggfunc=...)`와 ____________(이)다.

---

## **3.3 다중 인덱스 / 다중 컬럼 / 다중 값**

```python
pd.pivot_table(
    airbnb,
    index=[________, ________],            # 행 차원 2개
    columns='room_type',                    # 열 차원
    values=[________, ________],            # 두 종류의 값
    aggfunc={'price': ________, 'reviews': ________},  # 컬럼별 다른 함수
)
```

> 한 표 안에 ____________ 차원의 정보를 동시에 담을 수 있다.

---

## **3.4 `margins`와 `fill_value`의 활용**

```python
pd.pivot_table(
    airbnb,
    index='neighbourhood_group',
    columns='room_type',
    values='price',
    aggfunc='mean',
    fill_value=________,    # 결측 셀에 0
    margins=________,        # 전체 합계 행/열 추가
    margins_name='____________',
)
```

> margins의 합계 값은 단순 평균이 아닌 ____________________ 평균임에 주의한다.

---

## **3.5 `pivot` vs `pivot_table`**

| 메서드 | 집계 가능? | 중복 키 허용? | 마진 | 결측 처리 |
|---|---|---|---|---|
| `df.pivot()` | ____________ | ____________ | × | × |
| `pd.pivot_table()` | ____________ | ____________ | ○ `margins` | ○ `fill_value` |

> 실무에서는 ______________________이 훨씬 안전한 기본값이다.

---

## **3.6 다중 집계 함수 패턴**

```python
# 한 컬럼에 여러 함수
pd.pivot_table(airbnb, index='neighbourhood_group',
               values='price',
               aggfunc=[________, ________, ________])

# 컬럼별 다른 함수
pd.pivot_table(airbnb, index='neighbourhood_group',
               aggfunc={'price': ________, 'minimum_nights': ________})
```

> 사용자 정의 함수도 가능하다. `aggfunc=lambda s: ____________________`처럼 작성한다.

---

## **3.7 AI 연결 — 사용자-아이템 행렬**

추천 시스템의 ______________________ 행렬은 **사용자(row) × 아이템(col) → 평점(value)**의 `pivot_table`이다.

```python
ratings.pivot_table(
    index='____________',
    columns='____________',
    values='____________',
    aggfunc='mean',
    fill_value=____________,    # 평가하지 않은 셀
)
```

> 결측 비율이 매우 높은 ____________ 행렬이 만들어진다. 행렬 분해(Matrix Factorization)의 입력이 된다.

---

# **Part 4. `stack` / `unstack` / `melt` — 데이터 형태 변환**

## **4.1 wide vs long 형식**

| 형식 | 특징 | 용도 |
|---|---|---|
| **wide**(__________) | 컬럼이 ____________ 카테고리를 표현 | 보고용·표시용 |
| **long**(__________) | 컬럼이 ____________ 변수를 가짐 | 시각화·머신러닝 입력 |

```
[Wide]                          [Long]
국가  Jan  Feb  Mar              국가  월   값
KR  100  150  220                KR  Jan  100
JP   80  120  180     ←→          KR  Feb  150
                                  KR  Mar  220
                                  JP  Jan   80
                                  ...
```

> **AI 연결**: seaborn·plotly의 `hue`/`color` 인자는 사실상 ____________ 형식의 데이터를 요구한다. 머신러닝 학습 데이터도 일반적으로 ____________ 형식이 표준이다. 통계학에서는 이를 **____________________ data** 원칙이라 부른다.

---

## **4.2 `stack` / `unstack` — 인덱스와 컬럼의 자리바꿈**

```python
# 컬럼 → 인덱스 (wide → long, MultiIndex 활용)
df.____________()

# 인덱스 → 컬럼 (long → wide)
df.____________()
# 또는 특정 레벨만
df.unstack(level=____________)
```

> **암기 팁**: `stack`은 컬럼을 인덱스 ____________(아래)에, `unstack`은 인덱스를 컬럼 ____________(옆)으로 이동시킨다. 두 연산은 서로의 ____________ 연산이다.

### `groupby` + `unstack` 패턴

```python
# 다중 그룹 결과를 보기 좋은 표로
airbnb.groupby([________, ________])[________].mean().____________()
```

> 결과는 사실상 `pivot_table`과 동등하다.

---

## **4.3 `melt` — wide → long 변환의 정석**

```python
df.melt(
    id_vars=[__________________________],   # 그대로 유지할 컬럼 (식별자)
    value_vars=[__________________________],  # 녹여서 합칠 컬럼들
    var_name='____________',                # 변수 이름이 들어갈 컬럼명
    value_name='____________',              # 값이 들어갈 컬럼명
)
```

### 시각화 준비 패턴

```python
# wide 데이터를 long으로 녹여 hue로 그리는 패턴
long_df = wide_df.melt(id_vars='date', var_name='__________', value_name='__________')
sns.lineplot(data=long_df, x='date', y='__________', hue='__________')
```

> seaborn은 hue 변수를 long 형식의 ____________ 컬럼에서 찾는다.

---

## **4.4 `pivot` ↔ `melt` — 완전 왕복 변환**

```python
# wide → long
long = wide.melt(id_vars='id', var_name='var', value_name='val')

# long → wide
wide_back = long.____________(index='id', columns='____', values='____')
```

> 두 연산은 **____________**(__________) 관계이다. 즉, `melt` 후 `pivot`을 적용하면 (정렬 무시 시) 원본을 복구할 수 있다.

---

## **4.5 정리 — 언제 어떤 도구를 쓰는가?**

| 상황 | 도구 |
|---|---|
| 그룹별 임의 통계 | __________ |
| 그룹별 z-score / 정규화 (원본 길이 유지) | __________ |
| 두 범주형의 빈도/비율 표 | __________ |
| 다축 요약 + 다중 통계 + 마진 | __________ |
| 컬럼/인덱스 자리 바꿈 | __________ / __________ |
| 시각화·ML용 wide → long 변환 | __________ |
| long → wide 변환 (집계 없음) | __________ |
| long → wide 변환 (집계 필요) | __________ |
| 추천 시스템 사용자-아이템 행렬 | __________ |
| 분류 모델 혼동행렬 | __________ |

---

# **마무리**

## **30초 핵심 정리**

다음 빈칸을 30초 안에 모두 채워보라. 이것이 1차시의 핵심이다.

> 그룹별 임의 통계 → __________
>
> 두 범주형 빈도/비율 → __________
>
> 다축 요약의 표준 → __________
>
> wide → long → __________
>
> long → wide (집계 없음) → __________
>
> long → wide (집계 있음) → __________

---

## **자기 점검 — 주관식**

다음 질문에 답을 채워보라.

1. `groupby('a')['b'].mean()`과 `pivot_table(df, index='a', values='b')`의 결과 차이는?
   → ______________________________________________________________________

2. `crosstab(margins=True)`과 `pivot_table(margins=True)`의 합계 행은 단순 평균인가, 가중 평균인가?
   → ______________________________________________________________________

3. seaborn의 `hue` 인자는 wide 형식과 long 형식 중 어느 것을 요구하는가?
   → ______________________________________________________________________

4. 추천 시스템의 사용자-아이템 행렬은 long → wide 변환의 결과이다. 이 변환에 가장 적합한 함수는?
   → ______________________________________________________________________

5. `transform`이 `agg`와 다른 결정적 차이는?
   → ______________________________________________________________________

6. 분류 모델의 혼동행렬을 pandas로 직접 만들려면 어떤 함수를 사용하는가?
   → ______________________________________________________________________

7. wide 형식과 long 형식 중 'tidy data 원칙'을 따르는 것은?
   → ______________________________________________________________________

8. `pivot()`이 에러를 내는데 `pivot_table()`은 작동하는 상황은 어떤 경우인가?
   → ______________________________________________________________________

---

## **추가 도전 과제**(선택)

다음 과제를 노트북에 직접 구현해 보라. 정답은 노트북 토글에 포함되어 있지 않다 — **스스로 검증**해야 한다.

1. **자치구 × 가격 분위수 표 만들기**
   - `pd.cut`으로 `price`를 4분위로 나누고, 자치구별로 각 분위에 속하는 숙소 비율을 `crosstab`으로 작성하라.

2. **z-score 이상치 탐지**
   - 자치구별 가격 z-score를 `transform`으로 계산하고, |z| > 3인 이상치 비율을 자치구별로 집계하라.

3. **계절성 분석**
   - COVID-19 데이터에서 국가 × 월별 신규 확진자 평균을 `pivot_table`로 작성하고, `melt`로 long 형태로 바꾼 뒤 `sns.lineplot`으로 시각화하라.

---

> **다음 차시 예고**: 다음 차시에서는 본 차시의 집계·요약 결과를 시각적으로 표현하는 방법을 다룬다. **어떤 상황에서 어떤 그래프가 가장 적합한가**라는 질문을 중심으로, matplotlib·seaborn·plotly로 분포·관계·비교·시계열을 표현하는 기법을 학습한다.

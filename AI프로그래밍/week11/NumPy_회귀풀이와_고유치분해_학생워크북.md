# NumPy 선형대수 워크북 — 회귀 풀이 · 고유치 분해

**이름**: ____________  **학번**: ____________

핵심 개념과 코드의 *가장 중요한 부분*만 빈칸(`____`)으로 비워 두었다. 채우며 따라가고, 정답은 맨 끝에서 확인한다.

---

# 1부 · 회귀 풀이

## 개념 빈칸

- 선형회귀의 최적 계수는 한 줄로 구해진다: **β = ( (1) ____ )⁻¹ (2) ____ y** .
  (괄호 안은 XᵀX, 그 뒤는 Xᵀ)
- 설계행렬에 절편 β₀ 를 위해 **(3) ____** 로 채운 열을 맨 앞에 붙인다.
- 2×2 행렬식: `det([[a,b],[c,d]]) =` **(4) ____** .
- 행렬식이 **(5) ____** 이면 역행렬이 없어 정규방정식을 풀 수 없다.
- Cramer 법칙: β₀ = det(M₀) / det( **(6) ____** ), 여기서 M₀ 는 XᵀX 의 0번 열을 **(7) ____** 로 바꾼 행렬이다.

## 코드 빈칸

```python
import numpy as np
x = np.array([1, 2, 3, 4, 5], dtype=float)
y = np.array([3, 4, 6, 5, 7], dtype=float)

X    = np.column_stack([np.____(x), x])   # (8) 절편 열 + x 열
XtX  = X.____ @ X                          # (9) 전치 후 행렬곱
Xty  = X.T @ y
beta = np.linalg.____(XtX) @ Xty           # (10) 역행렬

yhat = X @ beta
r2   = 1 - ((y - yhat)**2).sum() / ((y - y.____())**2).sum()   # (11) 평균
```

## 계산 빈칸 (위 코드의 결과)

- `XᵀX` = `[[5, 15], [15, 55]]`, `det(XᵀX)` = **(12) ____** .
- `β` = **(13) [____, ____]** , 즉 ŷ = ____ + ____·x .
- `R²` = **(14) ____** .

---

# 2부 · 고유치 분해

## 개념 빈칸

- 고유치·고유벡터: 행렬을 곱해도 방향이 안 바뀌고 크기만 λ배 → **A v = (15) ____ v** .
- 대칭행렬(A=Aᵀ)은 고유치가 모두 실수이고, 서로 다른 고유치의 고유벡터가 **(16) ____** (한다).
- 고유벡터를 단위벡터로 정규화하면 Q 는 **(17) ____** 행렬이 되고, 따라서 **Q⁻¹ = (18) ____** 이다.
- 고유치 분해: **A = Q (19) ____ Qᵀ** (가운데는 고유치를 대각에 놓은 행렬).
- 압축: 큰 고유치 **(20) ____** 개만 남긴 랭크-k 근사로 원행렬(이미지)에 가까워진다.

## 코드 빈칸

```python
A = np.array([[2, 1],
              [1, 2]], dtype=float)        # 대칭행렬
w, Q = np.linalg.____(A)        # (21) 대칭 전용 (정규직교 고유벡터)

recon = Q @ np.____(w) @ Q.____  # (22) 고유치를 대각에 → 분해 되곱(복원)
# recon 은 원행렬 A 와 같다

# 대칭 이미지의 랭크-k 압축
w_img, Q_img = np.linalg.eigh(img)
order = np.argsort(np.abs(w_img))[::-1]      # 고유치 크기 내림차순
w_img, Q_img = w_img[order], Q_img[:, order]

def rank_k(k):
    return (Q_img[:, :k] * w_img[:k]) @ Q_img[:, :k].____   # (23) 전치
```

## 계산 빈칸 (소형 예 A=[[2,1],[1,2]])

- 고유치 λ = **(24) [____, ____]** .
- 각 고유벡터의 길이(노름) = **(25) ____** (정규화되어 있음).
- `QᵀQ` = **(26) ____** (직교행렬).
- `Q Λ Qᵀ` 로 복원하면 = **(27) ____** (원행렬).

---

## 정답

**1부**
1. XᵀX  2. Xᵀ  3. 1 (`np.ones_like`)  4. ad − bc  5. 0  6. XᵀX  7. Xᵀy
8. `ones_like`  9. `.T`  10. `inv`  11. `mean`
12. 50  13. [2.3, 0.9] → ŷ = 2.3 + 0.9·x  14. 0.81

**2부**
15. λ  16. 직교  17. 직교(orthogonal)  18. Qᵀ  19. Λ (고유치 대각행렬)  20. k
21. `eigh`  22. `diag` / `.T`  23. `.T`
24. [1, 3]  25. 1 (각 열의 노름이 1)  26. 단위행렬 `[[1,0],[0,1]]`  27. `[[2,1],[1,2]]` (원행렬 A)
